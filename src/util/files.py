
def generate_file(name:str root=None):

def write_reST_header(path, title: str, underline_with_char: str = '#',
                      top: bool = False):
    _file = open(path)

    _str = ''
    if top:
        _str = 
    _file.write(title)
